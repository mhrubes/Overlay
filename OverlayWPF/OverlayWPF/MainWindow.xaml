<Window x:Class="OverlayWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OverlayWPF"
        mc:Ignorable="d"
        Title="Overlay" MinHeight="350" Height="550" MinWidth="250" Width="350"
        AllowsTransparency="True" WindowStyle="None"
        MouseDown="Window_MouseDown" PreviewKeyDown="Window_PreviewKeyDown">

    <Window.Background>
        <SolidColorBrush Color="#FF363131" Opacity="{Binding ElementName=slider, Path=Value}"/>
    </Window.Background>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Slider x:Name="slider" Width="100" Margin="10" Value="87.5" Minimum="0.75" Maximum="1"/>

            <Button Grid.Column="1" Content="Vybrat soubor" Click="File_Button_Click" Width="100" Height="20" 
                    VerticalAlignment="Center" HorizontalAlignment="Right" Margin="5"/>
        </Grid>

        <TextBlock Grid.Row="1" Name="PathName" Foreground="White" Text="File Path" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0 0 5 0"></TextBlock>

        <Border Grid.Row="2" BorderThickness="0" BorderBrush="Transparent" Margin="0 5 0 0">
            <ScrollViewer MaxHeight="450"  
              Width="auto"
              HorizontalScrollBarVisibility="Disabled"
              VerticalScrollBarVisibility="auto">
                <TextBlock Name="textblockText" Foreground="White" TextWrapping="Wrap" VerticalAlignment="Top"></TextBlock>
            </ScrollViewer>
        </Border>

    </Grid>
</Window>
